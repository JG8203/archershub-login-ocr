{
  "name": "animlow-cortisol",
  "version": "1.0.1",
  "private": true,
  "description": "Chrome MV3 extension for local OCR on the Archers Hub login challenge image.",
  "scripts": {
    "build:assets": "node scripts/copy-assets.mjs",
    "build:bundles": "node scripts/build-bundles.mjs",
    "build": "npm run build:assets && npm run build:bundles",
    "check": "npm run build && node scripts/check.mjs",
    "package:release": "npm run build && node scripts/package-release.mjs"
  },
  "dependencies": {
    "@tesseract.js-data/eng": "^1.0.0",
    "tesseract.js": "^7.0.0"
  }
}
